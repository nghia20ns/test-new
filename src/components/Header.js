import React from 'react';

const header = () => {
    const headerStyle = {
        background: '#333',
        color: '#fff',
        textAlign: 'center',
        padding: '10px'
        }
    return (
            <header style={headerStyle}>
                    <h1>Work</h1>
            </header>
    )
};

export default header;
